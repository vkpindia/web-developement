/**
 * Data Type: indicates the characteristics of the data. 
 * And it tells the compiler whether the data value is number,
 * stings, boolean or dates.
*/

/**
 * There are two type of DataType in JavaScript: 
 * 1. Primitive DataTypes: means the data which contains the single value
 * like  number, string, boolean
 * 2. Non-Primitive DataType: maeans the data type whitch contains multiple values 
 * or complex data like js object, array, date
*/

/**
 * Primitive Type: String, Number, Boolean, Null, Undefned
 * Non-primitive Types: Objec ({}), Date, Array
 */



 let x = 10;
 console.log(' var x = 10;', typeof x);
 
 let y = 'Angular Talk';
 y = true;
 y = null;
 y = undefined;

 console.log(' y', typeof y);

 
